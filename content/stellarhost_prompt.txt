Write a brief, engaging description for curious general readers using the supplied evidence. Your task is conversion, not analysis: turn the supplied measurements, display text, and approved comparisons into flowing narrative.

Return valid Markdown: a single "#" title line, then connected paragraphs. Bold every occurrence of each planet's name and every supplied measurement value with its units, including the spectral type, like **about 1.51 days**, **TRAPPIST-1 b**, or **M8.0 V**. No lists, tables, links, code, or additional headings. Never use em dashes or double hyphens; a single hyphen is acceptable. Write plain, direct sentences; never re-define a guide term in apposition inside a sentence.

Treat the object names as labels. Every statement must come from system, star, planets, publishable_comparisons, or guide. Source fields are audit information and silent_constraints are private instructions. Familiarity with a name does not supply facts: omit remembered historical significance, brightness, composition, and orbital arrangement unless explicitly supplied. Never use remembered classifications such as "hot Jupiter" or host-type labels such as "pulsar"; use only the supplied classification and spectral facts. Never state that a planet or system is notable, pioneering, remarkable, or famous, and never characterize an orbit as tight, fast, or slow: state the supplied period and the approved comparisons instead.

Preserve each measurement's meaning, not just its number. Use the display value and qualifier. Mass provenance "Mass" means use the supplied mass estimate or bound; it does not mean minimum mass, even if discovery_method is Radial Velocity. Only provenance "Msini" licenses minimum-mass wording. An upper limit remains an upper limit, a lower limit remains a lower limit, and a radius remains a radius. For example, a supplied radius of about 1.2 Earth radii is written as "a radius of about 1.2 Earth radii", never "1.2 Earth radii across". Examples explain wording, not facts about this host.

The publishable_comparisons list is exhaustive. A comparison omitted from it must stay omitted, even when the point estimates seem to make it obvious. If a comparison you want is not listed, choose a different supported point rather than rephrasing, approximating, or deriving it. Do not reconstruct a suppressed comparison from raw values or uncertainty columns. State supplied orbital periods in days; use shorter/longer years only where approved. Do not translate periods into inner/outer positions, orbital distances, speed, or additional year conversions. For rankings, write years and sizes: say "the planet with the longest year" or "the largest reported radius", never "the outermost/innermost planet", "orbits closest in/farthest out", or "neighbors". Do not derive mass rankings or ratios.

Introduce the host with a few useful supplied facts, then name its associated planets and select measurements that distinguish them. The matching-host planet count and catalog-wide system count have different scopes. Explain a discovery method only if the guide supplies that explanation. Name order is not orbital order. Missing fields should simply be omitted, without commentary about unavailable data or conclusions about what cannot be inferred; never write that a value is unlisted, unreported, or unavailable.

Aim for the requested length but use fewer words when needed. Combine related facts rather than giving every planet the same inventory paragraph. Use each number once where practical. The title may be a short factual phrase describing the system, e.g. "<hostname>: six planets around a quiet star"; a plain "<hostname> and its planets" is also acceptable. Finish with the last useful supported fact; omit summaries, significance claims, metaphors, hype, and editorial cautions. Do not add headings that imply a physical arrangement.

Before responding, check each sentence against the supplied facts or an explicit approved comparison. Delete unsupported additions. Verify Mass versus Msini, every bound, radius wording, host counts, and that every comparative statement appears in publishable_comparisons or the guide. Keep this check private.
