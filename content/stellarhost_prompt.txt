Write a brief, factual catalog article for curious general readers using only the supplied JSON facts. Return one Markdown H1 title followed by connected paragraphs. No lists, tables, links, code, or extra headings. Formatting of names and measurements will be applied afterward.

Introduce the host with a few useful facts, then name every planet and select useful measurements for each. Integrate the exact-host planet count into the introduction without a separate inventory of names. Express counts naturally: never expose catalog bookkeeping such as a matching-host count, a catalog star count, or a listed-host distinction, and never turn a one-star count into a stellar companion. The length target is soft: shorter supported text is welcome. Group planets when helpful, and finish with the last useful fact rather than a recap.

Copy displayed numbers and units faithfully. State measurement values directly, without “about” or an added disclaimer. Preserve upper and lower limits. A mass field is an ordinary mass estimate or bound; only a minimum_mass field permits minimum-mass wording. Stellar measurements belong to the host star, not the whole multi-star system. Radius describes radius, not diameter. Size classifications describe radius, not composition.

The publishable_comparisons list is exhaustive. Comparisons are optional: select useful distinctions rather than reciting the list. State orbital periods in days. Omit all comparisons with Earth's year and all conversions to Earth years. Approved shortest/longest-period rankings among the listed planets remain optional. Never derive new rankings, ratios, orbital positions, proximity, or speeds, or extend one planet's comparison to a group. In particular, never call a planet most or least massive unless that exact ranking is supplied, and never rank ordinary mass against minimum mass.

Use guide explanations only when useful, at most once per concept or discovery method. Avoid repeated qualifiers, inventories, explanations, and sentence frames; group shared comparisons instead of repeating the same comparison for each planet. Say “minimum mass”, without raw audit notation. Silent constraints are private instructions, never article content. Do not add remembered facts, history, significance, or missing-data commentary; the explicitly supplied no-period fact is the sole missing-data exception. Apply every factual rule to the title as well as the body. Check the facts, grammar, and wording privately; remove tautologies before returning only the article.
