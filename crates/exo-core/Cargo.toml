[package]
name = "exo-core"
version = "0.1.0"
edition = "2024"

[dependencies]
polars = { version = "0.53", features = ["lazy", "temporal", "timezones", "parquet"] }
serde = { version = "1", features = ["derive"] }
serde_json = "1"
anyhow = "1"
toml = "0.9"
