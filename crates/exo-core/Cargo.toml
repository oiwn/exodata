[package]
name = "exo-core"
version = "0.1.0"
edition = "2024"

[dependencies]
polars = { version = "0.52", features = ["lazy", "temporal", "timezones", "parquet"] }
votable = "0.7"
serde = { version = "1", features = ["derive"] }
serde_json = "1"
anyhow = "1"
indicatif = "0.18"
